The ./audio folder does not exist yet.

Run  tools\make-audio.ps1  once (after installing a Tamil voice in Windows)
and it will be created, holding one recording per letter, syllable and word.
The app picks it up automatically on the next reload.

Without it the app falls back to the browser's own Tamil speech voice.
